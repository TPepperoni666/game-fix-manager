# Target platform is SteamOS — keep LF everywhere, even on Windows checkouts.
* text=auto eol=lf
# Payloads are shipped bytes — never normalize, diff, or touch them.
store/games/*/payload/** -text
*.exe binary
*.dll binary
*.asi binary
*.bin binary
