# Watch_Dogs payload — Living City mod (The Silver)

`mod/data_win64/` is copied into the game dir:

- `patch.dat` — the mod itself (274MB — NOT in git; auto-downloaded from the
  `payload-watch-dogs` release on first apply, sha256-verified)
- `patch.fat` — the mod's index file
- `Living_City_MOD_CONFIG.txt` — Tony's tuned config

Captured from a working Windows install (mod files dated 2025-04-06).
Upstream: The Silver — thesilver.it / ModDB.
